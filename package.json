
{
  "name": "latiabetina-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && nuxt build",
    "clean": "rm -rf .nuxt dist",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "vue": "latest",
    "vue-router": "latest"
  }
}
